# Third-party assets

현재 저장소에는 제3자 바이너리 에셋이 포함되어 있지 않습니다. UI는 시스템 폰트 폴백을 사용하고 핵심 SFX는 `AudioRouter`가 런타임에 합성하므로 별도 라이선스가 필요하지 않습니다.

향후 Noto Sans KR 또는 CC0 SFX를 추가할 때는 원본 URL, 정확한 버전/커밋, 라이선스 전문과 수정 여부를 이 디렉터리에 함께 기록해야 합니다.
